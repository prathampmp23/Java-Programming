spring.application.name=springboot_restapi
server.port=8080
# if below properties not added to connect JDBC mysql below mention error occurs
spring.datasource.url=jdbc:mysql://localhost:3306/college
spring.datasource.username=root
spring.datasource.password=#@Pratham23
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Error occur ->>
# Failed to configure a DataSource: 'url' attribute is not specified and no embedded datasource could be configured.
# Reason: Failed to determine a suitable driver class
# Action:
# Consider the following:
# 1. If you want an embedded database (H2, HSQL or Derby), please put it on the classpath.
# 2. If you have database settings to be loaded from a particular profile you may need to activate it (no profiles are currently active).